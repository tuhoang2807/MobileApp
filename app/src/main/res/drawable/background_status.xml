<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFF3CD" /> <!-- Màu nền (vàng nhạt) -->
    <corners android:radius="16dp" /> <!-- Bo góc -->
    <stroke android:width="1dp" android:color="@android:color/holo_orange_dark" /> <!-- Viền -->
</shape>
